- name: Install and configure Nginx
  hosts: web
  become: yes
  tasks:
    - name: Install Nginx
      apt:
        name: nginx
        state: latest
    - name: Ensure Nginx is running
      systemd:
        name: nginx
        state: started
        enabled: yes

