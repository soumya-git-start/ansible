- name: variables in files
  hosts: local
  connection: local
  vars_files:
    - course.yaml
  vars: #play level variable
    Course: "aws with devops"
    trainer: Shiva
    duration: 120hrs
  
  tasks:
  - name: print
    ansible.builtin.debug:
      msg: "i am learnin {{Course}}"
  
  - name: printing
    vars: #task level
      Course: cloud
    ansible.builtin.debug:
      msg: am learning {{Course}} 

 
        
       

      

